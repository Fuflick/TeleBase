@model IEnumerable<TeleBase.Models.BotCommand>

<h1>Bot Commands</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>BotId</th>
            <th>CommandId</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var botCommand in Model)
        {
            <tr>
                <td>@botCommand.Id</td>
                <td>@botCommand.BotId</td>
                <td>@botCommand.CommandId</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@botCommand.Id" class="btn btn-primary">Edit</a>
                    <form asp-action="Delete" method="post" style="display: inline;">
                        <input type="hidden" name="id" value="@botCommand.Id"/>
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
